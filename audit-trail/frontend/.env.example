# Only needed when the frontend is served from a different origin than the API.
# In development, vite.config.js proxies /api to localhost:4000, so the browser
# only ever makes same-origin requests and this can stay unset.
VITE_API_BASE_URL=
